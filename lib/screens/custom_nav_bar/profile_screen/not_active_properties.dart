import 'package:flutter/material.dart';

class NotActiveProperties extends StatelessWidget {
  const NotActiveProperties({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.white,
      body: Center(
        child: Text('Not Active Properties'),
      ),
    );
  }
}
